package com.prdv.notification.domain.model;

/** Canaux du module 5.1. Un nouveau canal = un nouvel adaptateur, pas de changement du coeur. */
public enum NotificationChannel {
    EMAIL,
    SMS,
    PUSH,
    WHATSAPP
}
